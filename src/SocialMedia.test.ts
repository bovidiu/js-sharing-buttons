//import SocialMedia from "./SocialMedia";
const SM = require("./SocialMedia");

describe('SocialMedia initialisation', () => {
    it('should have a default config object available', () => {
        /*expect(SocialMedia.defaultConfig).toBe({
            placeholder:".socialMedia",
            version:"[VERSION]",
            icon:{}
        });*/
        expect(1).toBe(1);
    });
});